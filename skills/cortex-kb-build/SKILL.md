---
name: cortex-kb-build
description: Explicit invocation only compatibility entry for Cortex KB build.
---

# Cortex KB build compatibility entry

Generic note, KB, or coding requests are insufficient. Read and follow exactly `../cortex/references/kb-build.md`; this entry preserves the existing name, launcher, runtime, and invocation policy.
